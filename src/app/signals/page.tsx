import { AppLayout } from '@/components/AppLayout';
import { SignalsPageContent } from '@/components/signals/SignalsPageContent';

export default function SignalsPage() {
  return (
    <AppLayout>
      <SignalsPageContent />
    </AppLayout>
  );
}
